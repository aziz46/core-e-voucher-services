# CMD Directory

This directory contains the main application entry points for each service.

## Structure

- `credit-service/` - Credit Limit Service main application
- `billing-service/` - Billing Service main application
- `ppob-core/` - PPOB Core Service main application

Each service has its own `main.go` file that bootstraps the service.
